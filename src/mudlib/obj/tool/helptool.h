/* Modificado por [i] Izet. 2 Oct 1997.
*/
#define NAME                 QueryRealName()

#define FAIL(x) return (notify_fail(x) && 0)
#include <wizlevels.h>
#include <properties.h>
#include <moving.h>
