// Comando version

int main(string str) {
    if (!TP) return 0;
    write("Gamedriver: LDmud "+__VERSION__+" (Amylaar+)\n");
    write("Mudlib: " + MUDNAME + " " + MUDVERSION + "\n");
    return 1;
}
