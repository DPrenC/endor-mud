//Nyh
//Creada para que no de un urg? cuando escribes esta accion y no tienes nada.

int main(string str) {
//  if (TP->QueryCombatDelay())
//    return notify_fail(this_player->QueryCombatDelayMsg());
  return notify_fail("¿Quieres nadar aquí?.\n");
}
